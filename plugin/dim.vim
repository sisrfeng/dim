com!   Di           lua   require("dim").toggle()
com!   Dim          lua   require("dim").toggle()
com!   DimEnable    lua   require("dim").enable()
com!   DimDisable   lua   require("dim").disable()
